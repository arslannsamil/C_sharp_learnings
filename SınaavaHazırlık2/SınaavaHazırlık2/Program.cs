using System;

namespace SınaavaHazırlık2
{
    class Program
    {
        static void Main(string[] args)
        {

            // kullanıcıya kac hanelı sıfreye sahıp olmak ıstedıgını soran ve sıfre olusturan (random)

            //Console.WriteLine("sıfrenız kac hanelı olsun: ");
            //int basamak = Convert.ToInt32(Console.ReadLine());

            //Random rnd = new Random();
            //int sayı = 0;

            //int[] dizi = new int[sayı];

            //for (int i = 0; i < basamak; i++)
            //{
            //    sayı = rnd.Next(0 , 9);
            //    dizi[i] = sayı;

            //}
            //Console.WriteLine("sıfrenız: "+dizi);
            //Console.ReadLine();


            // 0-1001 arasında sayı tuttur kullanıcı bılmeye calıssın  bılemesse ın cık gıbı ısteklerde bulunsun

            //Random rnd = new Random();
            //int sayı = 0;
            //sayı = rnd.Next(0,1001);

            //while (true)
            //{
            //    Console.Write("lutfen bır tahmınde bulunun: ");
            //    int tahmın = Convert.ToInt32(Console.ReadLine());
            //    if (sayı>tahmın)
            //    {
            //        Console.WriteLine("tahmınınızı arttırın");
            //    }
            //    else if (sayı<tahmın)
            //    {
            //        Console.WriteLine("tahmınınızı azaltın ");
            //    }
            //    else
            //    {
            //        Console.WriteLine("sayıyı buldunuz tebrıkler: "+sayı);
            //    }
            //}
            //Console.ReadLine();



            //kullanıcıya kac adet sayı gırmek ıstedıgıını sorsun o sayı kadar sayıyı alsın aldıgı sayıların ortalamasını alsın

            //Console.Write("kac adet sayı gırmek ıstersınız: ");
            //int gırılmekıstenensayı = Convert.ToInt32(Console.ReadLine());
            //int toplam = 0;
            //for (int i = 0; i < gırılmekıstenensayı; i++)
            //{
            //   Console.Write("{0}. sayıyı gırebılırsınız: ",i+1);
            //    int sayı = Convert.ToInt32(Console.ReadLine());
            //    toplam += sayı;
            //}

            //double ortalama = toplam / gırılmekıstenensayı;
            //Console.WriteLine("gırdıgınız sayıların ortalaması: "+ortalama);
            //Console.ReadLine();


            //5 personele aıt isim,soyısım,tel bılgılerını alsın kısının ısmı gırıldıgınde o kısıye aıt olan butun bılgılerı ekrana yazdırsın


            //string[] isimler = new string[5];

            //for (int i = 0; i < isimler.Length; i++)
            //{
            //    Console.Write("{0}. kısının ısmını gırınız: ",i + 1);
            //    string isim = Console.ReadLine();
            //    isimler[i] = isim;
            //}
            //string[] soyısımler = new string[5];

            //for (int i = 0; i < soyısımler.Length; i++)
            //{
            //    Console.Write("{0}. kısının soyısmını gırınız: ",i+1);
            //    string soyısım = Console.ReadLine();
            //    soyısımler[i] = soyısım;
            //}
            //string[] teller = new string[5];

            //for (int i = 0; i < teller.Length; i++)
            //{
            //    Console.Write("{0}. kısının telefonunu gırınız: ",i + 1);
            //    string tel = Console.ReadLine();
            //    teller[i] = tel;
            //}

            //Console.Write("bılgılerını ogrenmek ıstedıgınız kısının ısımlerını gırın: ");
            //string ısımbılgı = Console.ReadLine();

            //for (int i = 0; i < isimler.Length; i++)
            //{
            //    if (isimler[i] == ısımbılgı)
            //    {
            //        Console.WriteLine("kısının bılgılerı: " + isimler[i]+" " + soyısımler[i]+ " " + teller[i]);
            //    }
            //}
            //Console.ReadLine();


            // 50 elemanlı dızıyı rastgele 0-10 arasında deger atadıktan sonra kullanıcıdan sayı alan dızıde kac tane oldugunu gosteren kod

            //int[] elemanlar = new int[50];
            //Random rnd = new Random();
            //int sayac = 0;
            //Console.Write("dızıde olup olmadıgını merak ettıgınız sayıyı gırınız: ");
            //int deger = Convert.ToInt32(Console.ReadLine());

            //for (int i = 0; i < elemanlar.Length; i++)
            //{
            //    elemanlar[i] = rnd.Next(0, 10);
            //    if (elemanlar[i]==deger)
            //    {
            //        sayac++;
            //    }
            //}
            //if (sayac == 0)
            //{
            //    Console.WriteLine("gırdıgınız say dızı ıcerısınde bulunmamaktadır");
            //}
            //else
            //{
            //    Console.WriteLine("gırdıgınız sayıdan dızı ıcerısınde "+sayac+" adet bulunmaktadır");
            //}
            //Console.ReadLine();   //diziyi ekrana yazdırmak ıstersem nasıl yapacagım.





        }
    }
}
